# Nexus TV — Releases

Este repositório contém apenas os APKs publicados do Nexus TV e o `version.json` usado pelo
próprio aplicativo para checar atualizações. O código-fonte fica em um repositório privado
separado — nada aqui reflete a implementação, só os binários já compilados.
